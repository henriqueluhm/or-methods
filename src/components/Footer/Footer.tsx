import { ReactElement } from "react";

export default function Footer(): ReactElement {
    return <footer className="w-full h-[40px] flex items-center justify-center font-thin text-sm">developed by:</footer>
}